'use strict';(()=>{const slides=[...document.querySelectorAll('.slide')],mode=document.getElementById('slide-mode'),status=document.getElementById('slide-status');let i=Math.max(0,Math.min(slides.length-1,Number(location.hash.replace('#slide-',''))-1||0));function show(){slides.forEach((s,j)=>s.classList.toggle('active',i===j));status.textContent=`${i+1} / ${slides.length}`;history.replaceState(null,'',`#slide-${i+1}`);}function move(delta){i=Math.max(0,Math.min(slides.length-1,i+delta));show();if(document.body.classList.contains('presenting'))window.scrollTo(0,0);}mode.addEventListener('click',()=>{document.body.classList.toggle('presenting');mode.textContent=document.body.classList.contains('presenting')?'Način čitanja':'Način prezentacije';show();if(document.body.classList.contains('presenting'))window.scrollTo(0,0);});document.getElementById('slide-prev').addEventListener('click',()=>move(-1));document.getElementById('slide-next').addEventListener('click',()=>move(1));addEventListener('keydown',e=>{if(!document.body.classList.contains('presenting')||/INPUT|SELECT|TEXTAREA/.test(e.target.tagName))return;if(['ArrowRight','PageDown'].includes(e.key)){e.preventDefault();move(1);}if(['ArrowLeft','PageUp'].includes(e.key)){e.preventDefault();move(-1);}if(e.key==='Escape'){document.body.classList.remove('presenting');mode.textContent='Način prezentacije';mode.focus();}});show();})();
